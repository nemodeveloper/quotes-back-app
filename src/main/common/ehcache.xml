<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core.xsd">

    <persistence directory="ehcache.temp"/>

    <cache-template name="baseTemplate">
        <key-type>java.lang.String</key-type>
        <expiry>
            <none/>
        </expiry>
    </cache-template>

    <cache alias="categories" uses-template="baseTemplate">
        <resources>
            <heap unit="MB">50</heap>
            <disk unit="MB">100</disk>
        </resources>
    </cache>

    <cache alias="authors" uses-template="baseTemplate">
        <resources>
            <heap unit="MB">100</heap>
            <disk unit="MB">200</disk>
        </resources>
    </cache>

</config>